name: deploy-backend
description: Deploy backend API on push to main
github:
  owner: doylet
  name: PDF-OCR
  push:
    branch: ^main$
includedFiles:
  - backend/**
filename: backend/cloudbuild.yaml
