# API Configuration
# For local development, copy this to .env.local and update values

# Backend API URL - Required
NEXT_PUBLIC_API_URL=http://localhost:8000

# API Key for authentication - Optional (defaults to dev key if not set)
NEXT_PUBLIC_API_KEY=dev-api-key-change-in-production
